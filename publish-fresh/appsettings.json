{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "Comment": "Connection strings with credentials stored in User Secrets (development) or Azure Key Vault (production)"
  }
}

